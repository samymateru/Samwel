name: remote ssh command
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@v1.2.1
        with:
          host: "18.212.87.23"
          username: "ubuntu"
          key: ${{ secrets.KEY }}
          script: |
            cd project/Backend
            git pull
            sudo systemctl restart backed.service
